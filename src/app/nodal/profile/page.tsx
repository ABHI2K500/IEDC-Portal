"use client";

// The Nodal Officer shares the Execom screen; it adapts its links and headings
// to the /nodal prefix through useAdminSection().
import ExecomProfilePage from "@/app/execom/profile/page";

export default function NodalProfilePage() {
  return <ExecomProfilePage />;
}
