"use client";

// The Nodal Officer shares the Execom screen; it adapts its links and headings
// to the /nodal prefix through useAdminSection().
import ExecomProjectsPage from "@/app/execom/projects/page";

export default function NodalProjectsPage() {
  return <ExecomProjectsPage />;
}
