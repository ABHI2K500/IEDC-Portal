"use client";

// The Nodal Officer shares the Execom screen; it adapts its links and headings
// to the /nodal prefix through useAdminSection().
import ExecomEventsPage from "@/app/execom/events/page";

export default function NodalEventsPage() {
  return <ExecomEventsPage />;
}
