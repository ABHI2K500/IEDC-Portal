"use client";

// The Nodal Officer shares the Execom screen; it adapts its links and headings
// to the /nodal prefix through useAdminSection().
import ExecomSettingsPage from "@/app/execom/settings/page";

export default function NodalSettingsPage() {
  return <ExecomSettingsPage />;
}
